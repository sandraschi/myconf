[project]
name = "ag-visio-mcp"
version = "0.1.0"
description = "AG-Visio SOTA MCP Server"
dependencies = [
    "fastmcp>=2.14.4",
]

[project.scripts]
mcp-server = "mcp_server:mcp.run"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
